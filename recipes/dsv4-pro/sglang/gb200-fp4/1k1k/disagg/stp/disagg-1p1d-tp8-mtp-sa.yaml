# DeepSeek-V4-Pro disaggregated 1P1D on GB200 — TP=8 (2 nodes/worker), MXFP4,
# EAGLE MTP — sa-bench.
#
# Quick smoke-test sibling of disagg-1p1d-tp8-mtp-gpqa.yaml — same model,
# topology, parallelism, MTP, env vars, and KV transfer; only the benchmark
# differs. Use this first to confirm disagg + MTP plumbing serves traffic
# at all on the 4-node GB200 shape, then run the GPQA recipe for accuracy.
#
# See the GPQA sibling header for the rationale on Mooncake (NIXL bug) and
# sglang frontend (no jinja chat_template).
name: "dsv4-pro-gb200-disagg-1p1d-tp8-mtp-sa"

slurm:
  #partition: gb200
  time_limit: "2:00:00"

frontend:
  type: sglang

model:
  path: "dsv4-pro"
  container: "dsv4-grace-blackwell"
  precision: "mxfp4"

resources:
  gpu_type: "gb200"
  prefill_nodes: 2
  decode_nodes: 2
  prefill_workers: 1
  decode_workers: 1
  gpus_per_node: 4

backend:
  type: sglang

  prefill_environment:
    PYTHONUNBUFFERED: "1"
    SGLANG_JIT_DEEPGEMM_PRECOMPILE: "0"
    # Mooncake / MNNVL — required on GB200/GB300 for cross-node KV transfer.
    # Without these the prefill-decode bootstrap handshake hangs and every
    # request times out after 300 s with KVPoll.Bootstrapping. Pattern from
    # recipes/qwen3.5/fp8/disagg/mooncake/.../1p1d-dep4tp4-gpqa.yaml.
    NCCL_MNNVL_ENABLE: "1"
    NCCL_CUMEM_ENABLE: "1"
    MC_FORCE_MNNVL: "1"
    SGLANG_MOONCAKE_CUSTOM_MEM_POOL: "True"
    SGLANG_USE_MESSAGE_QUEUE_BROADCASTER: "0"
    SGLANG_DISABLE_TP_MEMORY_INBALANCE_CHECK: "1"
    SGLANG_DISAGGREGATION_HEARTBEAT_MAX_FAILURE: "100000"
    SGLANG_DISAGGREGATION_BOOTSTRAP_TIMEOUT: "100000"
    SGLANG_DISAGGREGATION_WAITING_TIMEOUT: "100000"
    TORCH_DISTRIBUTED_DEFAULT_TIMEOUT: "1800"

  decode_environment:
    PYTHONUNBUFFERED: "1"
    SGLANG_JIT_DEEPGEMM_PRECOMPILE: "0"
    NCCL_MNNVL_ENABLE: "1"
    NCCL_CUMEM_ENABLE: "1"
    MC_FORCE_MNNVL: "1"
    SGLANG_MOONCAKE_CUSTOM_MEM_POOL: "True"
    SGLANG_USE_MESSAGE_QUEUE_BROADCASTER: "0"
    SGLANG_DISABLE_TP_MEMORY_INBALANCE_CHECK: "1"
    SGLANG_DISAGGREGATION_HEARTBEAT_MAX_FAILURE: "100000"
    SGLANG_DISAGGREGATION_BOOTSTRAP_TIMEOUT: "100000"
    SGLANG_DISAGGREGATION_WAITING_TIMEOUT: "100000"
    SGLANG_DECODE_BOOTSTRAP_TIMEOUT: "1000"
    TORCH_DISTRIBUTED_DEFAULT_TIMEOUT: "1800"

  sglang_config:
    prefill:
      served-model-name: "deepseek-ai/DeepSeek-V4-Pro"
      model-path: "/model/"
      trust-remote-code: true
      tensor-parallel-size: 8

      disaggregation-mode: "prefill"
      disaggregation-transfer-backend: mooncake

      moe-runner-backend: "flashinfer_mxfp4"
      disable-flashinfer-autotune: true

      mem-fraction-static: 0.78
      max-running-requests: 128
      cuda-graph-max-bs: 128
      chunked-prefill-size: 8192
      disable-radix-cache: true

      speculative-algo: "EAGLE"
      speculative-num-steps: 3
      speculative-eagle-topk: 1
      speculative-num-draft-tokens: 4

    decode:
      served-model-name: "deepseek-ai/DeepSeek-V4-Pro"
      model-path: "/model/"
      trust-remote-code: true
      tensor-parallel-size: 8

      disaggregation-mode: "decode"
      disaggregation-transfer-backend: mooncake

      moe-runner-backend: "flashinfer_mxfp4"
      disable-flashinfer-autotune: true

      mem-fraction-static: 0.78
      max-running-requests: 128
      cuda-graph-max-bs: 128
      chunked-prefill-size: 8192
      disable-radix-cache: true

      speculative-algo: "EAGLE"
      speculative-num-steps: 3
      speculative-eagle-topk: 1
      speculative-num-draft-tokens: 4

benchmark:
  type: "sa-bench"
  isl: 1024
  osl: 1024
  random_range_ratio: 0.8
  concurrencies: "1x8x32"
  req_rate: "inf"
  use_chat_template: false
